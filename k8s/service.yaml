apiVersion: v1
kind: Service
metadata:
  name: dependency-radar
  namespace: dependency-radar
spec:
  type: ClusterIP
  selector:
    app: dependency-radar
  ports:
    - port: 80
      targetPort: 8080
